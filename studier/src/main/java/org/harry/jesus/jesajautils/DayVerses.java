package org.harry.jesus.jesajautils;

import java.util.Arrays;
import java.util.List;

public class DayVerses {

    public static List<BibleTextUtils.BookLink> verses =
            Arrays.asList(
                    new BibleTextUtils.BookLink(19,23,
                            Arrays.asList(1)),
                    new BibleTextUtils.BookLink(19,23,
                            Arrays.asList(5)),
                    new BibleTextUtils.BookLink(46,13,
                            Arrays.asList(13)),
                    new BibleTextUtils.BookLink(45,8,
                            Arrays.asList(37)),
                    new BibleTextUtils.BookLink(45,8,
                            Arrays.asList(38,39)),
                    new BibleTextUtils.BookLink(49,6,
                            Arrays.asList(2)),
                    new BibleTextUtils.BookLink(49,6,
                            Arrays.asList(18))
            );
}
